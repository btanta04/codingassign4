[project]
name = "assignment_922"
version = "0.1.0"
description = "Python assignment for September 22, 2025"
authors = ["Bisher Tanta <your.email@example.com>"]
dependencies = [
    "requests",
    "beautifulsoup4",
    "openai",
    "pandas",
    "supabase",
    "streamlit"
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"